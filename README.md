# Spray Caching Extensions #

A set of libraries that provide a number of additional caching libraries, for example:

- Memcached
- Couchbase

Currently the only implementation that has been performed is a memcached extension